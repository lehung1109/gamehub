# Implementation Plan: Complete Past Tenses

**Branch**: `021-complete-past-tenses` | **Date**: 2026-08-30 | **Spec**: F:\projects\gamehub\specs\021-complete-past-tenses\spec.md

**Input**: Feature specification from `F:\projects\gamehub\specs\021-complete-past-tenses\spec.md`

**Note**: This template is filled in by the `/speckit-plan` command; its definition describes the execution workflow.

## Summary

Add four new static JSON files for past tenses (`past-simple.json`, `past-continuous.json`, `past-perfect.json`, `past-perfect-continuous.json`) with IT/Workplace context challenges, and activate them in the `index.json` manifest.

## Technical Context

**Language/Version**: TypeScript 5.x (strict mode)

**Primary Dependencies**: Next.js 16.x, React 19.x

**Storage**: Static JSON data files (`src/data/tenses/`)

**Testing**: Vitest + Testing Library, Playwright

**Target Platform**: Web (Next.js App Router)

**Project Type**: Next.js Web App

**Performance Goals**: Fast loading of static JSON files

**Constraints**: IT/Workplace context only, 80 challenges per file (20 of each category), UI must handle data without code changes.

**Scale/Scope**: 4 new JSON files (320 total challenges) and 1 manifest update.

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **Next.js App Router**: Compliant. (Data static files)
- **TypeScript-First**: Compliant. (Data validation tests required)
- **Component-Driven UI**: Compliant. (Relies on existing UI)
- **Test-First**: Compliant. (Requires schema validation tests to pass)
- **Task Generation Standards**: N/A for planning, applies to task generation.

## Project Structure

### Documentation (this feature)

```text
specs/021-complete-past-tenses/
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
└── tasks.md             # Phase 2 output (generated by /speckit-tasks)
```

### Source Code (repository root)

```text
src/
└── data/
    └── tenses/
        ├── index.json
        ├── past-simple.json
        ├── past-continuous.json
        ├── past-perfect.json
        └── past-perfect-continuous.json

tests/
└── data/
    └── tenses.test.ts   # For schema validation
```

**Structure Decision**: The feature primarily involves adding static data files to the existing `src/data/tenses/` directory and ensuring tests cover them.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

No violations detected.
